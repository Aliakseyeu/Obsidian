
```sql
INSERT LOW_PRIORITY INTO table
```

- вставка **ждёт**, пока таблица не освободится от `SELECT`
- актуально в основном для **MyISAM**
- для **InnoDB почти не имеет смысла**

⚠️ В InnoDB чаще **бесполезен**