
```php
Model::find()  
    ->select('id')  
    ->where(['column' => $column])  
    ->scalar();
```

- `scalar()` вернёт **одно значение** (первый столбец первой строки)
- Если записи нет — вернёт `false`