
```php
Yii::$app->queue->ttr(1000)->push(new MyJob);
```

# 🔍 Что делает `ttr(1000)`

`ttr` = **Time To Run** — максимальное время, сколько worker может выполнять эту задачу.

### То есть:

**задача должна выполниться за 1000 секунд.**

Если задача:

- **выполняется дольше 1000 секунд** → worker считает её зависшей и помечает как failed или отправляет повторно
- **завершится раньше** → всё ОК