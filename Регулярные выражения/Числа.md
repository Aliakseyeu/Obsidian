```php
preg_match_all('/\d+/', $comment, $matches);
```

### Что делает:

- `\d+` — находит любое число (одно или больше цифр).
- «Разделённые любым символом» — означает, что между числами могут быть любые буквы, пробелы, спецсимволы. Регулярка просто найдёт все отдельные числовые фрагменты.