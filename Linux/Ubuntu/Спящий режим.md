
### Посмотреть список настроек

```bash
gsettings list-recursively org.gnome.settings-daemon.plugins.power
```

---

### Изменить настройку

```bash
gsettings set org.gnome.settings-daemon.plugins.power sleep-inactive-ac-timeout 0
```

---

### Посмотреть список возможных значений

```bash
gsettings range org.gnome.settings-daemon.plugins.power lid-close-ac-action
```

## Значения `lid-close-*-action`

### 🔲 `blank`

**Погасить экран**, система продолжает работать
- CPU работает    
- программы работают    
- экран просто выключен  📌 Подходит, если ноутбук подключён к монитору    
### 😴 `suspend`

**Спящий режим (Suspend / Sleep)**
- состояние сохраняется в RAM    
- минимальное энергопотребление    
- быстрое пробуждение    📌 Самый популярный режим по умолчанию

### ⛔ `shutdown`

**Полное выключение компьютера**
- все процессы завершаются    
- питание отключается    ⚠️ Редко используется для крышки — опасно при случайном закрытии

### 🧊 `hibernate`

**Гибернация**
- состояние сохраняется на диск (swap)    
- питание полностью отключается    
- медленнее пробуждение   📌 Работает только если настроен swap

### 🧑‍💻 `interactive`

**Показать диалог выбора действия**
- GNOME спрашивает: что сделать?    
- может предложить сон / выход / выключение   📌 Почти бесполезно для lid switch (крышка уже закрыта)

### 🚫 `nothing` ✅

**Ничего не делать**
- система продолжает работать    
- экран гаснет аппаратно    
- НЕТ сна, НЕТ гибернации  📌 **Лучший вариант**, если:    
- ноутбук всегда с внешним монитором    
- используется как стационарный ПК
### 🚪 `logout`

**Выход из сессии пользователя**
- приложения закрываются    
- пользователь разлогинен   ⚠️ Редко используется, возможна потеря данных
