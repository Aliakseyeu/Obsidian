#linux [[Grep]]
### Поиск с учетом маски имени файл
```bash
find . -name '*pattern*' -exec grep -r -A 3 --color 'pattern' {} +
```

```bash
grep -C 10 "pattern" /path/to/file
```
### Поиск в архивах

[[Циклы]]