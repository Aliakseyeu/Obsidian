#linux [[Grep]]
### Поиск с учетом маски имени файл
```bash
find . -name '*pattern*' -exec grep -r -A 3 --color 'pattern' {} +
```

```bash
grep -C 10 "pattern" /path/to/file
```
### Поиск в архивах

[[For]]

---

Эта команда найдет найдет строки с двумя вхождениями и подсветит вхождения

```bash
grep -iE --color=always 'some_text' access.log | grep -E --color=always 'another'
```

[[Подсветка вхождений]]

---

Поиск внутри архива

```bash
zgrep -i 'my/url' access.log.*.gz
```