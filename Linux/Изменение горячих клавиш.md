## 🔹 Шаг 1. Посмотреть текущие привязки

В GNOME все хоткеи для рабочих столов лежат тут:

`gsettings list-recursively org.gnome.desktop.wm.keybindings | grep workspace`

Там будут строки типа:

`org.gnome.desktop.wm.keybindings switch-to-workspace-left   ['<Super>Page_Up'] org.gnome.desktop.wm.keybindings switch-to-workspace-right  ['<Super>Page_Down'] org.gnome.desktop.wm.keybindings switch-to-workspace-1      ['<Super>1']`

---

## 🔹 Шаг 2. Отключить ненужную комбинацию

Чтобы убрать конкретное сочетание, сбрось его в пустой список:

`gsettings set org.gnome.desktop.wm.keybindings switch-to-workspace-left "[]" gsettings set org.gnome.desktop.wm.keybindings switch-to-workspace-right "[]"`

То же самое можно сделать для `switch-to-workspace-1`, `switch-to-workspace-2` и т.д.

---

## 🔹 Шаг 3. Проверка

После изменения комбинация должна перестать работать сразу (иногда нужно перезапустить сессию или разлогиниться).