
nginx.conf
```bash
location ~ \.php$ {  
    fastcgi_read_timeout 300s;   # ждать ответа от PHP до 300 секунд  
    fastcgi_send_timeout 300s;   # ждать отправки запроса к PHP  
    client_max_body_size 50M;    # если загружаются большие файлы  
}
```